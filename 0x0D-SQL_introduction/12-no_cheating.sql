-- Updates the score of Bob in the table second_table of the database hbtn_0c_0 in your MySQL server
-- Use only the field name to update the record
-- The database name will be passed as an argument of the mysql command

UPDATE second_table SET score = 10 WHERE name = "Bob";
