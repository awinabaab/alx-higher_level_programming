#!/usr/bin/python3
"""A class that defines a square with a size.
"""


class Square:
    """A class that defines a square with a size.
    """
    def __init__(self, size):
        """Initialize a private instance field size

        Args:
            size: the size of the instance(private)
        """
        self.__size = size
